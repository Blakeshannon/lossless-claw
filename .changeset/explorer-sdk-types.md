---
"@martian-engineering/lossless-claw": patch
---

Check the LCM panel against OpenClaw's public SDK types instead of a locally maintained structural subset, without adding host runtime code to the browser bundle.
